shell/.yarnrc

# Environment variables (sensitive data)
.env

# Generated git configuration (contains sensitive data)
.gitconfig.local

gh/hosts.yml

# OS generated files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# Personal customizations (don't commit these)
shell/.dotfiles-custom/
.npmrc

# Agent tool configs (symlinked at install, not tracked here)
claude/
opencode/
